export const state = () => ({});

export const mutations = {};

export const actions = {
  async nuxtServerInit() {
    try {
      ///
    } catch (error) {
      this.$snack.show(error, 'danger');
    }
  },
};
